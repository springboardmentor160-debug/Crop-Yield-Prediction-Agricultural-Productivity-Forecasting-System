{
  "name": "yieldsense-backend",
  "version": "3.0.0",
  "description": "YieldSense AI — FastAPI Backend",
  "private": true,
  "scripts": {
    "dev": "venv\\Scripts\\uvicorn main:app --reload --host 0.0.0.0 --port 8000",
    "start": "venv\\Scripts\\uvicorn main:app --host 0.0.0.0 --port 8000",
    "train": "venv\\Scripts\\python -m ml.train",
    "install-deps": "venv\\Scripts\\pip install -r requirements.txt"
  }
}
